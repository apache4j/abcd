spring:
  main:
    allow-bean-definition-overriding: true
  application:
    name: @artifactId@
  cloud:
    nacos:
      username: ${NACOS_USERNAME:}
      password: ${NACOS_PASSWORD:}
      discovery:
        server-addr: ${NACOS_HOST:192.168.27.165}:${NACOS_PORT:8848}
        namespace: ${spring.profiles.active}
        group: ${spring.profiles.active}
      config:
        server-addr: ${spring.cloud.nacos.discovery.server-addr}
        namespace: ${spring.profiles.active}
        group: ${spring.profiles.active}
        file-extension: yml
        shared-configs:
          - { dataId: "common-config.${spring.cloud.nacos.config.file-extension}" , group: "${spring.profiles.active}", refresh: true }
          - { dataId: "${spring.application.name}.${spring.cloud.nacos.config.file-extension}", group: "${spring.profiles.active}", refresh: true }

