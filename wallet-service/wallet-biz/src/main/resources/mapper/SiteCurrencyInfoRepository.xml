<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cloud.baowang.wallet.repositories.SiteCurrencyInfoRepository">

    <update id="updateBatchList" >
        <foreach collection="voList" item="vo" separator=";">
            update site_currency_info
            set
            plat_currency_code=#{vo.platCurrencyCode},
            plat_currency_name=#{vo.platCurrencyName},
            plat_currency_symbol=#{vo.platCurrencySymbol},
            plat_currency_icon=#{vo.platCurrencyIcon},
            final_rate =#{vo.finalRate}
            where currency_code=#{vo.currencyCode}
        </foreach>
    </update>

</mapper>
